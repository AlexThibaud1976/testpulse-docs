---
title: "Report history & collaboration"
description: "Auto-saved history, compare two reports, the Draft to Approved workflow, and sharing to the project."
icon: "history"
---

Every generated report is **auto-saved** to your Azure DevOps Extension Data (user-scoped, 5 MB quota, **synchronised cross-browser** — your history follows you to any machine). Attachments are stripped before saving and re-fetched on restore.

## Reload a report

Click **Load** on any history entry and TestPulse opens the **Report** tab with that report displayed. The snapshot is used **directly — no re-fetch from Azure DevOps**.

The context is put back with it: the report's test plan is selected again and its suites are re-checked, so you can regenerate or adjust the scope without rebuilding the selection by hand. A suite deleted from Azure DevOps since the report was built is skipped silently. A report consolidating several test plans switches the view to multi-plan mode and ticks all of its plans — multi-plan mode offers no suite selection, so none is restored there. Reports saved before this behaviour existed reload just as well: the plan and suites are read from the report itself.

## Which project a report came from

Personal history is stored per user across the **whole organisation**, so anyone working on several projects sees them all in one list. A **Project** column sits immediately after Test plan, and a **Project** filter sits between the status filter and *My reports*. It opens on your **current project**, or on **All projects** when that one has nothing to show. The header count switches to *{n} of {total} reports* whenever a project filter is active.

Reports saved since v2.63.0 record their project. Older ones are **derived at display time** from the Azure DevOps links they already carry — underlined with a dotted rule, naming their source on hover, so a derived value never carries the authority of a stored one. Where neither is possible they are grouped under **Unknown project**, an entry of its own with its count. Nothing is dropped, and no report is rewritten.

## Compare two reports

Select **two** reports from history to compare them side by side:

- **Metrics diff** — pass-rate delta, totals, passed / failed / blocked / bugs.
- **Improved** — tests that moved to Passed.
- **Regressed** — tests that moved from Passed to Failed / Blocked.
- **Added / Removed** — test cases new to, or missing from, the newer report.

Click **Export HTML** for a standalone comparison file.

## Status workflow

Each report carries a **status**: **Draft → Submitted → Approved** (and back via **Reopen**). Colour-coded badges, contextual **Submit / Approve / Reopen** buttons, every transition recorded (user + timestamp), a filter by status, and an optional **APPROVED badge** in the PDF footer.

<Note>Only users with the right role should Approve. Reopen resets the status to Draft and logs a history entry.</Note>

## Share to the project

Toggle **Share with project** to make a report visible to all project members (a snapshot copied into the project scope). A blue **Shared** badge appears next to the plan name, with a **Shared by** line under it carrying the sharer's initials and name — your own reports read *Shared by you*. You can filter *My / Project / All*, and **any member** can move a shared report through its workflow (written to the project copy, audited who / when). Only the **original author** can unshare. A **quota indicator warns at 80%**.

<Note>Sharing publishes the snapshot that the dashboard widget reads — see the [Quality overview widget](/en/dashboard/quality-overview-widget).</Note>

## CSV, notes & storage

Export all history as **CSV**, keep a **500-character note** per report, and watch the **storage indicator** (green → red above 80% of quota). Delete old reports to free space.

## Related

<CardGroup cols={2}>
  <Card title="Notifications & scheduled reports" icon="bell" href="/en/collaboration/notifications-and-scheduling">Push KPIs, get reminded.</Card>
  <Card title="Quality overview widget" icon="layout-dashboard" href="/en/dashboard/quality-overview-widget">Where a shared report surfaces.</Card>
</CardGroup>